block flex-layout {
    js: true,

    content: {
        applyNext(
            this.orient = this.ctx.mods.orient,
            this.theme = this.ctx.mods.theme || this.theme,
            this.ctx = this.ctx.content,
            '');
    }
}